# Frontend deployment

Upload these files to the root of a GitHub repository:

- index.html
- dashboard.html
- styles.css
- app.js

Then enable GitHub Pages.

Important: edit `app.js` and set `API_BASE` to your Cloudflare Worker URL.
